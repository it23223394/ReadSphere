[phases.setup]
aptPkgs = ["openjdk-17-jdk", "maven"]

[phases.install]
cmds = ["cd backend && mvn dependency:go-offline -B"]

[phases.build]
cmds = ["cd backend && mvn clean package -DskipTests"]

[start]
cmd = "cd backend && java -jar target/*.jar"